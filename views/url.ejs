<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <title>Url Shortner</title>
  </head>
  <body class="bg-success bg-gradient text-white" style="height: 100vh">
    <div class="container py-5">
      <h1>Url Shortner</h1>
      <p>
        A Custom URL Shortener using Node.JS and MongoDB. Setting up the server,
        connect to a MongoDB database, and implement the functionality to
        shorten and redirect URLs.
      </p>
      <form action="" method="post">
        <label class="form-label">Enter Full URL</label>
        <input type="url" class="form-control" name="redirectURL" required />
        <button type="submit" class="btn btn-warning fw-medium mt-4">
          Submit
        </button>
      </form>
      <table class="table table-striped table-bordered table-responsive mt-4">
        <thead>
          <tr>
            <th scope="col">Full Url</th>
            <th scope="col">Short Url</th>
            <th scope="col">Total Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% data.map((item,i)=>{ %>

          <tr key="<%= i %>">
            <th>
              <a href="<%=item.redirectURL%>" class="link-success"
                ><%=item.redirectURL%></a
              >
            </th>
            <th>
              <a href="/url/<%=item.shortId%>" class="link-success"
                ><%=item.shortId%>
              </a>
            </th>
            <th><%=item.visitHistory.length%></th>
          </tr>
          <%}) %>
        </tbody>
      </table>
    </div>
  </body>
</html>
